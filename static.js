const STORE = {
  tokenKey: Symbol('tokenKey'),
};

const RESPONSE_STATUS = {
  OK: '0',
  ERROR: '1',
};

const AUTHORITY_TOKEN = 'authority_token_key';

export { STORE, RESPONSE_STATUS, AUTHORITY_TOKEN };
